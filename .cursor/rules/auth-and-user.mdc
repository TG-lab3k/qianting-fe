---
description: 认证与用户状态、HttpClient 初始化约定
globs: "**/core/auth/**/*.ts,**/core/user/**/*.ts,**/app/HttpClientInit.tsx,**/app/login/**/*.tsx"
alwaysApply: false
---

# 认证与用户状态约定

## 登录链路

1. 前端：Firebase `signInWithGoogle()` 取得 Firebase User，`getIdToken(user)` 得到 id_token。
2. 调用 `authLogin(idToken)`（POST `/auth/login`），后端返回 access_token 与用户信息。
3. 前端：`saveAuthToStorage(token, name, avatar)` 写入 sessionStorage；`getUserManager().login({ token, name, avatar })` 更新内存状态。
4. 跳转至 callbackUrl（如 `/`）。

## 应用启动恢复

- `HttpClientInit` 在根 layout 中挂载，仅在客户端执行：
  - 用 `getStoredToken()` / `getStoredUser()` 从 sessionStorage 恢复；
  - 若有 token 则 `getUserManager().setUser({ token, name, avatar })`；
  - 再 `initHttpClient({ getToken: () => getUserManager().getToken() })`，之后所有请求自动带 `Authorization: Bearer <token>`。
- 不在 HttpClientInit 之外重复调用 initHttpClient；token 来源统一为 UserManager（其初值由 storage 恢复）。

## 用户状态在 React 中的使用

- 内存中的登录态由 `getUserManager()` 单例维护（token、name、avatar）。
- 需要响应登录/登出的组件使用 `useUser()` 订阅；登出时调用 `authLogout()` 并 `clearAuthFromStorage()`、`getUserManager().logout()`。

## 关键导出

- **auth**：`authLogin`、`authLogout`、`authMe`；`saveAuthToStorage`、`clearAuthFromStorage`、`getStoredToken`、`getStoredUser`。
- **user**：`getUserManager()`、`useUser()`；UserManager 提供 `login`、`logout`、`onLogin`、`onLogout`。
