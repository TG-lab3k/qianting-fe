---
description: 认证与用户状态、HttpClient 初始化约定
globs: "**/core/auth/**/*.ts,**/core/user/**/*.ts,**/app/HttpClientInit.tsx,**/app/login/**/*.tsx"
alwaysApply: false
---

# 认证与用户状态约定

## 登录链路

1. 登录页：`oauthAuthorize(redirectUri)` → 整页跳转 `authorization_url`（Google）。
2. 回调页 `/login/callback`：用 `code` 调 `oauthCallback` → access + refresh。
3. 前端：`saveAuthToStorage(access, refresh, name, avatar)` 写入 localStorage；`getUserManager().login({ token, name, avatar })`。
4. `router.replace(consumePendingCallbackUrl())`（OAuth 前由 `setPendingCallbackUrl` 写入 sessionStorage）。

## 应用启动恢复

- `HttpClientInit` 在根 layout 中挂载，仅在客户端执行：
  - 用 `getStoredToken()` / `getStoredUser()` 从 localStorage 恢复；
  - 若有 token 则 `getUserManager().setUser({ token, name, avatar })`；
  - 再 `initHttpClient({ getToken: () => getUserManager().getToken() })`；
  - 有 token 时 `authMe()`；若 1007/401 且存在 refresh → `authRefresh` 后再 me；仍失败则清态。
- 认证请求使用请求级 `baseURL: WACHI_AUTH_BASE`；业务请求走默认 `API_BASE`。
- 不在 HttpClientInit 之外重复调用 initHttpClient；token 来源统一为 UserManager。

## 用户状态在 React 中的使用

- 内存中的登录态由 `getUserManager()` 单例维护（token、name、avatar）。
- 需要响应登录/登出的组件使用 `useUser()` 订阅；登出时调用 `authLogout()` 并 `clearAuthFromStorage()`、`getUserManager().logout()`。

## 关键导出

- **auth**：`oauthAuthorize`、`oauthCallback`、`authRefresh`、`authLogout`、`authMe`；storage / session / config helpers。
- **user**：`getUserManager()`、`useUser()`；UserManager 提供 `login`、`logout`、`onLogin`、`onLogout`。
