---
description: UI 组件与样式、Tailwind 主题约定
globs: "**/components/ui/**/*.tsx,**/app/globals.css,**/lib/utils.ts"
alwaysApply: false
---

# UI 与样式约定

## 技术栈

- **Tailwind CSS v4**：主题与 design tokens 在 `src/app/globals.css` 中通过 `@theme` 与 `:root` 定义（字体、颜色、阴影、圆角、动画等）。
- **组件风格**：shadcn 风格——使用 Radix UI 插槽、class-variance-authority（CVA）做变体、`cn()`（来自 `@/lib/utils`）合并 className。

## 组件写法

- 通用 UI 放在 `src/components/ui/`，使用 CVA 定义 variants（如 variant、size），用 `cn(buttonVariants({ variant, size, className }))` 合并类名。
- 与现有 design tokens 保持一致：如 `--color-text-primary`、`--color-border`、`--radius-md` 等，避免硬编码颜色或圆角。

## 工具函数

- 类名合并统一用 `cn(...inputs)`（`lib/utils.ts` 中基于 clsx + tailwind-merge），保证 Tailwind 类可正确覆盖。

## 字体

- 字体由根 layout 通过 next/font 注入 CSS 变量（如 `--font-plus-jakarta-sans`），在 globals.css 中映射为 `--font-sans` / `--font-mono` / `--font-serif`；组件与页面引用这些变量或已有工具类即可。
